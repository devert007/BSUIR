#pragma once
#include <string>
#include <Contact.h>
#include <vector>
using namespace std;
class OnlinePlatform {
private:
	string name;
	string orientation;
	//vector<Comment> comments;
	vector<Contact> contacts;
};